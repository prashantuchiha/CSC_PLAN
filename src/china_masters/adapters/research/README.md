# Future research adapters

Phase 2 includes `FakeResearchProvider`, which returns deterministic synthetic candidates and
makes no network requests. Future real providers implement the application research port and
are injected in `bootstrap.py`. See `docs/architecture/future-integrations.md`.
